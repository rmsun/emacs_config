#include <stdio.h>

void 
insertion_sort(int(*t)[], n)
{
     int i, key;
     for (i = 1; i < n - 1; i++)
     {
          key = t[i];
          while (t[i] < t[i - 1] && i >= 0)
          {
               
